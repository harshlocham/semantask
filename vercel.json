{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "framework": "nextjs",
    "installCommand": "corepack enable && pnpm install --frozen-lockfile",
    "buildCommand": "pnpm exec turbo run build --filter=@semantask/web",
    "outputDirectory": "apps/web/.next"
}