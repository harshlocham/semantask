import { WorkspaceLayout } from "@/components/shell/workspace-layout";

export default function SuggestionSectionLayout({ children }: { children: React.ReactNode }) {
    return <WorkspaceLayout>{children}</WorkspaceLayout>;
}
